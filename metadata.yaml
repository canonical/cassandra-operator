name: cassandra
display-name: Apache Cassandra
description: TODO

requires:
  client-certificates:
    interface: tls-certificates
    limit: 1
    optional: true
  peer-certificates:
    interface: tls-certificates
    limit: 1
    optional: true