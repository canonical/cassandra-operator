summary: test_cos.py
environment:
  USES_K8S: "true"
execute: |
  tox run -e integration-cos -- --alluredir="$SPREAD_TASK/allure-results" --model-name=test-cas --keep-models
artifacts:
  - allure-results
